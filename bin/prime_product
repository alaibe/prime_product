#!/usr/bin/env ruby

require_relative '../lib/prime_product'

options = PrimeProduct::OptParser.parse(ARGV)
primes  = PrimeProduct::Prime.new.first options.number
product = PrimeProduct::Product.new(primes).calculate
PrimeProduct::Report::IoFormater.new(primes, product).generate_and_display